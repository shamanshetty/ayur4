import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
});

export const metadata: Metadata = {
  title: "AyurSutra - Your Journey to Health Begins",
  description: "AyurSutra bridges traditional Panchakarma with smart, patient-centered technology. Connect with certified Ayurvedic practitioners for personalized healing.",
  keywords: "Ayurveda, Panchakarma, Traditional Medicine, Health, Wellness, Practitioners",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${playfair.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
